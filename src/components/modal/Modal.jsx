import React from 'react'
import PropTypes from 'prop-types'
import './modal.scss'

const Modal = props => {
    return (
        <div>
            
        </div>
    )
}

Modal.propTypes = {
    active: PropTypes.bool,
    id: PropTypes.string
}

export default Modal
